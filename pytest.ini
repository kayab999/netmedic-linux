[pytest]
testpaths = tests
markers =
    ai: tests that require the optional netmedic_ai package
    netns: requires netns harness (root)
filterwarnings =
    always::DeprecationWarning
    error:NetResult.*:UserWarning