# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
*.egg
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/
.pyre/

# Virtual environments
venv/
.venv/
env/

# Environment & IDE
.env
.env.*
.idea/
.vscode/
*.swp
.DS_Store

# Build & distribution artifacts
build/
dist/
*.spec.bak
NetMedic.AppDir/
NetMedic-*.AppImage
netmedic_run

# Runtime state (local dev)
*.log
*.pid
*.sock
netmedic.log
# Soak evidence is host-specific (absolute paths, hashes, live eventlog) — not committed.
# Keep the template in docs/; per-run SOAK_*.md + golden logs stay local.
SOAK_PLAN.md
SOAK_EVENTLOG.md
golden_*.log

# AI model weights (download separately — see docs/DEVELOPMENT.md)
*.gguf
*.sum

# Local packaging caches
localpycs/netmedic_run

# Mutation testing sandbox + cache (mutmut 3.x)
mutants/
.mutmut/
